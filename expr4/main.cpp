#include <iostream>
#include <cmath>
using namespace std;

double target(double x) {
    return 2 / sqrt(PI) * (1 - exp(-1));
}

double Runberg() {
    double h, a, b;
    double T;

    double precise = target

    // T0 = [f(a) + f(b)] / 2
    T = (func(a) + func(b)) / 2;

}

int main() {



    
    return 0;
}